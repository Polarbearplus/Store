<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper  namespace="com.cy.store.mapper.AddressMapper">

    <insert id="insert">
        INSERT INTO t_address (
        uid, name, province_name, province_code, city_name, city_code, area_name, area_code, zip,
        address, phone, tel,tag, is_default, created_user, created_time, modified_user, modified_time
    ) VALUES (
        #{uid}, #{name}, #{provinceName}, #{provinceCode}, #{cityName}, #{cityCode}, #{areaName},
        #{areaCode}, #{zip}, #{address}, #{phone}, #{tel}, #{tag}, #{isDefault}, #{createdUser},
        #{createdTime}, #{modifiedUser}, #{modifiedTime}
    )
    </insert>
    <update id="updateNonDefault">
        UPDATE t_address
        SET is_default = 0
        WHERE uid = #{uid}
    </update>
    <update id="updateByAid">
        UPDATE t_address
        SET
        is_default = 1,
        modified_time = #{modifiedTime},
        modified_user = #{modifiedUser}
        WHERE aid = #{aid}
    </update>
    <delete id="deleteByAid">
        DELETE FROM t_address where aid = #{aid}
    </delete>
    <select id="countByUid" resultType="java.lang.Integer">
         SELECT
    	COUNT(*)
    FROM
   		t_address
    WHERE
    	uid=#{uid}
    </select>
    <select id="selectByUid" resultType="com.cy.store.entity.Address">
        select * from t_address where uid = #{uid}
    </select>
    <select id="findAllByUid" resultType="com.cy.store.entity.Address">
        SELECT uid,aid,tag, name, address, phone FROM t_address
        WHERE uid = #{uid}
        ORDER BY is_default DESC, created_time DESC
    </select>
    <select id="findByAid" resultType="com.cy.store.entity.Address">
        SELECT * FROM t_address
        WHERE aid = #{aid}
    </select>
    <select id="findLastModified" resultType="com.cy.store.entity.Address">
        SELECT *FROM t_address
        WHERE uid = #{uid}
        ORDER BY modified_time DESC
        LIMIT 0,1
    </select>
</mapper>